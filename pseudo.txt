STRING Str
INT index

FUNCTION TypeWriter()
    IF index < length of string - 1
        print character at index in str
        increment index
        call TypeWriter() after 100ms
    END IF

TypeWriter()

================================================================================================

INT currentcharacter
INT storefulltext[]
INT currentsentence
INT typingspeed
INT totalcharacters
INT blinkcursorate = 500ms

FUNCTION TypeWriter()
    INT totalcharacters = num of chars in storefulltext[In all elements]
    INT storenumberofsentences = number of elements in storefulltext[]
    IF currentcharacter < totalcharacters
        PRINT currentcharacter
        blinkcursor()
        INCREMENT currentcharacter 
        IF currentcharacter > number of characters in ArrayElement storefulltext[currentsentence]
            PRINT <br/>
            INCREMENT currentsentence
        END IF        
    ELSE
        RETURN 
    END IF
    WAIT for typingspeed
    TypeWriter()
END FUNCTION

FUNCTION blinkcursor()
    IF currentcharacter is not printed on the screen
    PRINT an underscore "_"
    WAIT blinkcursorate
    DELETE the PRINTED underscore (currentcharacter - 1)
END FUNCTION    

TypeWriter() // Will this get called after TypeWriter() exit in code.j





